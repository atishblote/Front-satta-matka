import { LiveBazarPipe } from './live-bazar.pipe';

describe('LiveBazarPipe', () => {
  it('create an instance', () => {
    const pipe = new LiveBazarPipe();
    expect(pipe).toBeTruthy();
  });
});
